#ifndef NFS_NEXT_GEN_CLIENT_H
#define NFS_NEXT_GEN_CLIENT_H

#include "GameData.h"
#include "Converter.h"

class Client {
private:
//    std::string playerName;
//    socket _socket;
//    Converter converter;
//    void writeRequest(Request &request);
//    void readAnswer(Response &response);
//    void connect(endpoint ep);
//    GameData convertStrToData(string &str);
//    std::string convertDataToStr(GameData &data) :
public:
//    void runClient(endpoint ep, std::string playerName);
//    std::string getUserName();
//    void getGameData(GameData &data);
//    void sendGameData(GameData &data);
};

#endif //NFS_NEXT_GEN_CLIENT_H
