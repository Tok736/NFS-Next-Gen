#ifndef NFS_NEXT_GEN_HOSTSESSION_H
#define NFS_NEXT_GEN_HOSTSESSION_H

#include "GameData.h"

class Session {
private:
//    socket _socket;
//    char _data[max_lenght];
//    void handle_read(boost::system::error_code& err, size_t bytes_transfered);
//    void handle_write(boost::system::error_code& err);
public:
//    void handle_write(boost::system::error_code& err);
//    std::string getName();
//    GameData getGameData();
//    handle_read(error&, size_t byte_transferred);
};

#endif //NFS_NEXT_GEN_HOSTSESSION_H
